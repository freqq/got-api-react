import loader from 'assets/loader.gif';

import { LoaderWrapper } from 'components/Loader/Loader.styles';

const Loader = () => <LoaderWrapper alt="loader-wrapper" src={loader} />;

export default Loader;
